default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  username: postgres
  password:
  pool: 5

development:
  <<: *default
  database: expenses_menager_db


test:
  <<: *default
  database: expenses_menager_test